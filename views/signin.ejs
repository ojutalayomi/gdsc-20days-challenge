<!DOCTYPE html>
<html>
    <head>
        <title>Sign In</title>
        <style>
            body {
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh;
                flex-direction: column;
            }

            form {
                display: flex;
                justify-content: center;
                gap: 30px;
                flex-direction: column;
                align-items: center;
            }

            input,button {
                height: 50px;
                width: 60vw;
                border-radius: 10px;
                padding: 0 10px;
                font-size: 1.5em;
                outline: none;
            }
        </style>
    </head>
    <body>
        <h1> Your One Stop Bookstore </h1>
        <h2> Sign into account with us. </h2>
        <form action="/accounts/signin" method="post" target="_blank">
            <input type="email" name="email" placeholder="Fill in your email" required>
            <input type="password" name="password" placeholder="Fill in your password" required>
            <button type="submit">Sign In</button>
        </form>
    </body>
</html>